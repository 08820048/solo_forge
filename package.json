{
  "name": "soloforges",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:admin": "npm --prefix admin-frontend run dev -- -p 3002",
    "build": "npm --prefix frontend run build",
    "build:admin": "npm --prefix admin-frontend run build",
    "lint": "npm --prefix frontend run lint"
  }
}
